# Terraform state files (never commit)
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.*

# Terraform working directory
.terraform/
.terraform.lock.hcl
.terraform.tfstate.lock.info

# Terraform debug logs
terraform-debug.log

# Provider binaries (Google, Random, etc.)
terraform-provider-*
*.exe

# Local variable files (private)
*.tfvars
*.auto.tfvars

# OS/Editor junk files
.DS_Store
Thumbs.db
.vscode/
.idea/

# Python or shell scripts cache (if any)
__pycache__/
*.log
